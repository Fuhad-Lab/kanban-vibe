# Kanban Vibe

Target repo for the zero-to-deployed benchmark. The platform's coding worker builds the app; the static smoke contract in tests/smoke.js is the verification gate; GitHub Pages serves the result.
